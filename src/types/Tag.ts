export interface Tag {
    $collectionId: string;
    $createdAt: string;
    $databaseId: string;
    $id: string;
    $permissions: string[];
    $updatedAt: string;
    tag_description: string;
    tag_icon: string;
    tag_name: string;
}